import preStyles from "@/constants/GlobalStyles";
import { View } from "react-native";

export default function MediaGenreCard() {
    return(
        <View style={[ preStyles.w48, preStyles.h15, preStyles.bgGray, preStyles.rounded10]}>

        </View>
    );
}