import preStyles from "@/constants/GlobalStyles";
import { View } from "react-native";

export default function SmallMediaCard() {
    return (
        <View style={[ preStyles.h35, preStyles.w47, preStyles.bgGray, preStyles.rounded10 ]}>
        
        </View>
    );
}
