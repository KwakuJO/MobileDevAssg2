import preStyles from "@/constants/GlobalStyles";
import { View } from "react-native";

export default function VideoClipsCard() {
    return(
        <View style={[ preStyles.w36, preStyles.hFull, preStyles.bgGray, preStyles.rounded10]}>

        </View>
    );
}